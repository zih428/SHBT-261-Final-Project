[runtime]
seed = 7
log_level = "INFO"
output_root = "outputs/runs"
cache_root = "data/cache"
device_order = ["mps", "cuda", "cpu"]
num_workers = 2
run_tag = "mps-tuned-v1"

[training]
run_tag = "train-speed-v2"
